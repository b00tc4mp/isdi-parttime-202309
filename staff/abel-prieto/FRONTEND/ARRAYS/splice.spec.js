// El método splice() cambia el contenido de un array eliminando elementos existentes y/o agregando nuevos elementos.

var array = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday']

console.log('TEST ARRAYS - Method Splice')

console.log('CASE for the array, replace the element on array[2] "Wednesday" with the string "Summer"')
// Expected output: ['Monday', 'Tuesday', 'Summer', 'Thursday', 'Friday', 'Saturday', 'Sunday']

// console.log(splice(2, 1, "Summer"))


console.log('- - - - - - - - - - - - - - - - - - - - - - - -')