function contarLetras(string) {
    for (var i = 0; i < string.length; i++) {
        //mi primer clg es para ver la posicion que se esta realizando cada vez que hace el for
        console.log([i]);
        console.log(string[i]);
        //guardaLetras sirve para ir guardando la letra de cada for en una variable
        var guardaLetras = string

    }
    //este clg es para ver la palabra del string entera y fuera del for o ciclo en este caso y solo 1 vez
    console.log(string);

    // esta siguiente interacoin para ver la cantidad guardaLetras es lo mismo que string.lenght
    console.log(string.length);
    return console.log(guardaLetras.length);

}
var totaLetras = contarLetras('Hola')




