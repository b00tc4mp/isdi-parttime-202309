// El método forEach() ejecuta la función indicada una vez por cada elemento del array.

var array = [10, 20, 30]

console.log('TEST ARRAYS - Method forEach')

console.log('CASE for each element, print one by one on console')

var elementsFound = forEach(array)
console.log(elementsFound)

console.log('- - - - - - - - - - - - - - - - - - - - - - - -')